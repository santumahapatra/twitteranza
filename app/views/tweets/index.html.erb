<h1>What are you doing?</h1>
<div class="form-container">
  <%= render 'form' %>
</div>

<h1>The World in Tweets</h1>

<ul class="tweets_box">
  <% @tweets.each do |tweet| %>
    <li class="tweet">
      <%= tweet.content %>
    </li>
  <% end %>
</ul>
